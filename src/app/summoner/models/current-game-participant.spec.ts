import { CurrentGameParticipant } from './current-game-participant';

describe('CurrentGameParticipant', () => {
  it('should create an instance', () => {
    expect(new CurrentGameParticipant()).toBeTruthy();
  });
});
